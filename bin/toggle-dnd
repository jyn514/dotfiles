#!/bin/sh

case ${1:-} in
	true) new=false;;
	false) new=true;;
	*)
	current=$(gsettings get org.gnome.desktop.notifications show-banners) || exit
	if [ "$current" = true ]
		then new=false
		else new=true
	fi
	;;
esac
gsettings set org.gnome.desktop.notifications show-banners $new
