#!/bin/sh
# note: the `cd` makes tmux open helix in the right working directory
metadata=$(cargo metadata --format-version 1) || exit
workspace=$(printf '%s\n' "$metadata" | jq .workspace_root -r) || exit
cd "$workspace" || exit
exec bacon "$@"
