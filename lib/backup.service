# sudo cp lib/backup.* /etc/systemd/system
# sudo chown root:root /etc/systemd/system/backup.*
# sudo systemctl enable backup.{service,timer}
# journalctl --unit backup

[Unit]
Description="Backup google drive"
Wants=network-online.target
After=network-online.target

[Service]
Type=oneshot
ExecStart=/home/jyn/src/dotfiles/bin/backup 
User=restic

[Install]
WantedBy=multi-user.target
